echo betterscript parser/compiler running...

if [ $# -ne 2 ]
then
	echo "Usage: `basename $0` sourcefile targetfile"
	exit -1
fi

ometa bs.ometa compilationUnit source $1 > $1.ast
ometa bsc.ometa ast ast $1.ast > $2